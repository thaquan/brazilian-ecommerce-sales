# Bronze Dataflow

`templates/DF_Olist_Bronze.pqt` is the sanitized Power Query template exported from Fabric. The decoded folder contains the M document and metadata for nine source queries plus their destination queries.

Import the template into an empty Dataflow Gen2 project, then select connections and destinations again. Credentials, schedules, and connection definitions are not included in a Power Query template.
