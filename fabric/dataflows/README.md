# Dataflow Bronze

`templates/DF_Olist_Bronze.pqt` là template gốc; `DF_Olist_Bronze/MashupDocument.pq` chứa toàn bộ Power Query để review. `brz_reviews.pq` là bản sửa riêng trước đây, không phải toàn bộ Dataflow. Xem [review](../../docs/fabric-export-review.md).
