# Pipeline exports

Template ZIP gốc ở `templates/`; JSON giải nén theo từng tên pipeline để review trên GitHub. Xem [hướng dẫn import và các khác biệt](../../docs/fabric-export-review.md). Không import mù cả E2E và staging: E2E template đã nhúng resource staging.
